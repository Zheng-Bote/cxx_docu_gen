# Architecture Documentation - cxx_docu_gen

## Overview

The `cxx_docu_gen` is a C++23-based documentation tool that extracts Doxygen comments and SPDX headers from source files and generates structured Markdown documentation.

---

<!-- DOCTOC SKIP -->
<!-- START doctoc -->

---

## Bounded Context Diagram

```mermaid
graph TD
    User((Developer)) -- Input Path --> CLI[CLI Application]
    CLI --> Scanner[Filesystem Scanner]
    Scanner -- File Paths --> Parser[Regex Parser Engine]
    Parser -- Doc Model --> Generator[Markdown Generator]
    Generator -- Markdown Files --> FS[(Filesystem)]
    Generator -- Completion Event --> Indexer[Index Generator]
    Indexer -- README.md --> FS
```

## Component Diagram

The system consists of three main components:

- **Parser Engine (`parser_util`)**: Responsible for reading file contents and using regex patterns to extract documentation metadata.
- **Markdown Generator (`markdown_generator_util`)**: Responsible for formatting the extracted metadata into human-readable Markdown.
- **CLI Orchestrator (`main`)**: Manages the overall workflow, including directory scanning and execution of the parser and generator.

```mermaid
flowchart TD
    subgraph "cxx_docu_gen System"
        MO[Main Orchestrator] --> PE[Parser Engine]
        MO --> MG[Markdown Generator]
        PE -.-> DM[Doc Models]
        MG -.-> DM
    end
```

## Sequence Diagram

```mermaid
sequenceDiagram
    participant CLI as Main Orchestrator
    participant SC as Scanner
    participant PR as Parser Engine
    participant GN as Markdown Generator
    participant IX as Index Generator

    CLI->>SC: Scan 'src' and 'include'
    SC-->>CLI: List of files
    loop for each file
        CLI->>PR: parse_file(path)
        PR-->>CLI: FileDoc Model
        CLI->>GN: generate_markdown(FileDoc)
        GN->>FS: Write .md file
        CLI->>CLI: Store FileDoc in list
    end
    CLI->>IX: generate_index(List<FileDoc>)
    IX->>FS: Write README.md index
    CLI-->>User: Completed
```

## Class Diagram (Data Models)

```mermaid
classDiagram
    class FileDoc {
        +string file_name
        +string relative_path
        +DocHeader header
        +vector~DocEntry~ entries
    }
    class DocHeader {
        +string spdx_comment
        +string spdx_type
        +string spdx_contributor
        +string spdx_license_id
        +string file_name
        +string description
        +string version
        +string date
        +string author
        +string copyright
        +string license
    }
    class DocEntry {
        +string signature
        +string brief
        +vector~pair~string, string~~ params
        +string returns
    }
    FileDoc "1" *-- "1" DocHeader
    FileDoc "1" *-- "*" DocEntry
```

## Design Decisions

- **C++23 Standard**: Utilizes modern features like `std::print` for performance, `std::expected` for robust error handling, and `std::filesystem` for platform-agnostic file operations.
- **Regex-based Parsing**: Provides a lightweight alternative to full AST parsing while remaining effective for structured comments (Doxygen/SPDX).
- **Separation of Concerns**: Parsing logic is completely decoupled from formatting logic, allowing for future support of different output formats (e.g., HTML, JSON).
- **Index Generation**: Added in v1.1.0 to provide a central navigation point for the generated documentation.

---

_Generated by cxx_docu_gen (Self-Documented)_
